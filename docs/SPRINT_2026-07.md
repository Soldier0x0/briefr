# Sprint — July 2026

**Purpose:** single working checklist for everything agreed in the July
planning sessions (strategy, refactor, error handling, data utilization,
UI/UX). Work top to bottom inside each track; tracks A–C are sequential,
D–F can interleave. Tick items as they merge; add the PR number next to
each ticked box. When a checked item changed runtime behavior, update
`docs/PRODUCT_STATUS.md` in the same PR (see `CLAUDE.md` docs rules).

**Definition of done for every item:** tests pass (`pytest` + `npm run
build`), verified in the browser where UI is touched, docs updated per
`CLAUDE.md`, one PR per item unless marked otherwise.

---

## Track A — Quick wins: close the error-handling loop

- [ ] **A1. Merge PR #255** (strategy, learning path, CLAUDE.md, doc cleanup,
      embeddings dead-code fix).
- [ ] **A2. Capture `X-Request-ID` in `frontend/src/api.js`.** Attach
      `err.requestId = res.headers.get('X-Request-ID')` in `request()`.
      *Done when:* a thrown API error carries `requestId`.
- [ ] **A3. App-wide error toast.** Promote the admin `Toast` component out of
      `pages/admin/shared/` into a shared component; every failed fetch in
      analyst tabs shows one styled toast: API `detail` + `ref: <request-id>`.
      The ref is a link to `/admin` logs with `?request_id=` pre-filled — the
      log viewer already supports that filter (`IngestLogPage.jsx`).
      *Done when:* forcing a 500 (e.g. temporary test route) shows a toast
      whose ref link opens the matching backend log lines.
- [ ] **A4. `PoolExhaustedError` handler returns a fixed message** ("Server
      busy — please retry shortly") instead of `str(exc)`; detail stays in
      the log. (`backend/main.py`.)
- [ ] **A5. Error-state pass over analyst views.** Every async view renders a
      designed error state (message + ref + retry button) — no silent
      failures, no raw exception text. Inventory first, then fix.

## Track B — Structural refactor (`docs/REFACTOR_PLAN.md`)

Verified accurate 2026-07-03 (line tables match code). Rules: **one phase =
one PR = one deploy**; full `pytest` + `npm run build` green before the next
phase; phases 4–5 additionally verified by hand in the browser (drawer tabs,
PDF/XLSX export). Never proceed past a red check.

- [ ] **B1. Phase 1** — CVE ID validator helper (~25 lines).
- [ ] **B2. Phase 2** — scheduler lock consolidation.
- [ ] **B3. Phase 3** — split `database.py` into `db/` package (shim keeps
      all imports working). The risky one — review the diff carefully.
- [ ] **B4. Phase 4** — split `DetailDrawer.jsx` into folder.
- [ ] **B5. Phase 5** — consolidate export utilities.

**After Track B (separate, not July-scoped):** extend the CI Postgres job
(pattern already in `backend-tests.yml`) to run the full test suite, then
convert `db/` modules to Postgres-native SQL one PR at a time; delete
`db/dialect.py` translation last.

## Track C — Data utilization audit ("nothing fetched goes to waste")

Principle: maximize use of data we already download; drop what nothing
consumes. Audit before building.

- [ ] **C1. Source→field utilization matrix.** For each ingest source
      (NVD, KEV, EPSS, Vulnrichment, cvelistV5, OTX, CIRCL, OSV, exploit
      indices, Nuclei), list: fields fetched → fields stored → fields
      surfaced in UI/scoring. Output: a table in this file (or
      `TECHNICAL_INVENTORY.md`). Known suspects to check first:
      Vulnrichment SSVC decisions, KEV `knownRansomwareCampaignUse`,
      CIRCL CAPEC links, cvelistV5 ADP containers, Nuclei template bodies.
- [ ] **C2. Surface the top 3 unused high-value fields** found in C1
      (each its own small PR — e.g. ransomware-use badge on KEV cards if
      not already shown, SSVC decision in the drawer).
- [ ] **C3. Retention/TTL review.** Confirm every cache table (ioc_cache,
      feed_cache, negative caches) has a TTL or cleanup path; drop stored
      data nothing reads. *Done when:* no table grows unboundedly without
      a consumer.

## Track D — Detection quality (first rungs of the STRATEGY §4 ladder)

- [ ] **D1. CWE→template mapping in `sigma_generator.py`.** When a CVE has
      no ATT&CK technique, select the template by CWE class instead of
      falling to the generic default (CWE-22→path traversal patterns,
      CWE-78→command injection, CWE-89→SQLi, CWE-502→deserialization).
      *Done when:* a technique-less CVE with a known CWE gets a specific
      rule, with tests.
- [ ] **D2. (stretch) Nuclei artifact injection spike.** Parse the Nuclei
      template for one CVE and inject its HTTP paths/params into the
      generated Sigma rule; evaluate quality before generalizing.

## Track E — UI/UX (against `PRODUCT.md` principles, not decoration)

- [ ] **E1. States audit.** Table of every analyst-facing component ×
      (loading / empty / error / data); fill the gaps — starting with the
      BRIEF-tab dead zone next to the 90-day heatmap.
- [ ] **E2. Stat tile deltas.** Each hero tile shows Δ vs previous day and
      clicking pre-filters the feed.
- [ ] **E3. Tooltip coverage pass.** Every badge/pill/stat label gets a
      discoverable explanation (PRODUCT.md principle 1) — start with
      "EXPLOITED IN WILD" and the filter toggles.
- [ ] **E4. IOC input auto-detect.** Paste anything; detect IP/hash/domain;
      remove the manual type tabs.
- [ ] **E5. Small craft fixes.** Word-boundary truncation on CVE cards;
      heatmap legend adjacent to heatmap; hide the LIGHT toggle until the
      light theme ships.
- [ ] **E6. (stretch) Cmd+K command palette** — jump to CVE, run IOC lookup,
      switch tabs, trigger refresh.

## Track F — Foundations for later (start, don't finish, in July)

- [ ] **F1. ADR for the `intel`/`app` schema split** (sellable-data boundary;
      use `docs/TEMPLATE_adr.md`). Design only — implementation after
      Track B lands.
- [ ] **F2. License decision recorded** (STRATEGY §6: AGPL-3.0 recommended)
      + draft `CONTRIBUTING.md` + `.github/FUNDING.yml` ready for the
      open-source flip. No flip until beta feedback is in.

## Track G — Maintainer learning (parallel, personal)

- [ ] **G1–G4.** `docs/LEARNING_PATH.md` Modules 1–4 (request path, data
      layer, ingest/scheduler, scoring) — one per week, with written notes.

## Track H — Reusable UI component layer (added 2026-07-06)

Consolidate the duplicated UI primitives (state blocks, modals, toast,
tooltips, buttons, badges) into `frontend/src/components/ui/` per Spec H
in the appendix. H is the structural vehicle for A3, A5, E1, E2 and E3 —
when an H item merges, tick the corresponding A/E item with the same PR
number. H interleaves with D–F; H4 must coordinate with B4 (DetailDrawer
split) so the drawer is not churned twice.

**Verify code first (applies to every H item):** the duplication
inventory behind this track was taken 2026-07-06. Before implementing an
item, re-read the named files/components against current code — if they
moved, were split (Track B4), or already gained the behavior, adjust the
item to reality and note the deviation next to the checkbox instead of
executing stale instructions (appendix rule: stop and flag, don't
improvise).

- [ ] **H1. Request-id + app-wide toast.** Execute Spec A2+A3 as written,
      plus the design-token additions from Spec H §2. *Done when:* the
      Spec A2+A3 verification passes.
- [ ] **H2. `ui/` primitives.** Button, Badge, Tooltip, Skeleton,
      ErrorState, EmptyState, AsyncState + `useAsync` hook (APIs in
      Spec H §4). Proof-of-fit: the BRIEF-tab dead zone (E1's named
      starting point) renders through `AsyncState`. *Done when:*
      `npm run build` green + keyboard-only browser pass of the
      migrated view.
- [ ] **H3. States-audit fill (= A5 + E1).** Produce the analyst-view ×
      (loading/empty/error/data) table, then wire every gap through
      `AsyncState`/`ErrorState` (message + `ref: <request-id>` + retry).
      CVEFeed migrates its state blocks only — infinite scroll and
      keyboard nav are feature logic and stay put.
- [ ] **H4. Modal consolidation.** `Modal` (on `useModalLayer`) +
      `ConfirmModal` composing it; rebase `PdfExportModal` and the admin
      `ConfirmModal` (which today lacks focus trap and overlay-depth
      registration) onto them. `DigestModal`/`AboutModal` follow only if
      the diff stays mechanical.
- [ ] **H5. Badge/Tooltip coverage (= E3).** `Badge` requires an
      `explain` prop rendering a `Tooltip` (hover + focus + touch, not
      raw `title=`). Start with "EXPLOITED IN WILD" and the filter
      toggles per E3.
- [ ] **H6. `Field` + `StatTile` (= E2).** Form-field wrapper with auto
      `htmlFor`/`aria-invalid`/`aria-describedby` wiring; `StatTile`
      promoted from admin `StatCard` with `delta` + `onClick` props —
      the vehicle for E2's hero-tile deltas and click-to-filter.

---

## Weekly rhythm

End of each week: tick merged items here (with PR numbers), update
`docs/PRODUCT_STATUS.md` if behavior changed, and pick the next week's
items. Do not start a new track while an item in the same track is red.

---

# Appendix — Implementation specs (pre-decided; execute as written)

Design decisions below are settled — implementing agents should follow
them rather than re-deriving. If a spec conflicts with code reality,
stop and flag it instead of improvising.

## Spec A2+A3 — request-id error loop

1. `frontend/src/api.js`, in `request()` where the error is thrown:
   attach `err.requestId = res.headers.get('X-Request-ID') || ''` and
   keep `err.status`.
2. Move `frontend/src/pages/admin/shared/Toast.jsx` (both `useToast` and
   `ToastArea`) to `frontend/src/components/Toast.jsx`; update the admin
   imports (`AdminPage.jsx` and any other `shared/Toast` importers). Keep
   the existing 180 ms leave animation and styles.
3. Mount one `ToastArea` at app level (`App.jsx`); expose a
   `showApiError(err)` helper that renders: the API `detail` message,
   plus — when `err.requestId` is non-empty — a `ref: <id>` suffix that
   links to the admin ingest-log page with `?request_id=<id>` prefilled
   (the page already reads that filter).
4. Wire analyst-tab fetch failures (feed, brief, IOC lookup, detail
   drawer, incidents) to `showApiError`. Do not remove existing designed
   inline error states — the toast is additive for transient failures.
5. Tests/verification: `npm run build`; in the browser, stop the backend
   and confirm each tab shows the toast with a ref id once backend
   returns 500s (or add a temporary dev-only failing route).

## Spec A4 — pool-exhausted message

`backend/main.py` `pool_exhausted_handler`: keep the warning log as-is,
change the response to
`{"detail": "Server is busy — please retry in a few seconds."}` (503).
Do not include `str(exc)` in the response.

## Spec D1 — CWE→Sigma template mapping (design decided)

`cwe_ids` is already stored on `cves` (JSON list, e.g. `["CWE-22"]`) —
no ingest work needed. In `backend/detection/sigma_generator.py`:

1. Add `CWE_TEMPLATES: dict[str, dict]` keyed by CWE id, same shape as
   `TECHNIQUE_TEMPLATES`. Selection order in `generate_sigma_rule`:
   technique template → CWE template (first CWE with a mapping) →
   `DEFAULT_TEMPLATE`. Add a `briefr_basis` field to the rule output:
   `"attack_technique" | "cwe" | "generic"` so the UI can show why this
   template was chosen (PRODUCT.md principle 1).
2. Mapping table (validated starting set — extend, don't invent levels):

| CWE | Class | Logsource | Detection core | Level |
|---|---|---|---|---|
| CWE-22, -23, -35 | Path traversal | `webserver` | keywords: `../`, `..\\`, `%2e%2e%2f`, `%252e`, `/etc/passwd`, `boot.ini`, `web.config` | high |
| CWE-78 | OS command injection | `webserver` | keywords: `;id`, `|whoami`, `` `id` ``, `$(`, `&&+wget`, `;curl+`, `/bin/sh`, `cmd.exe+/c` | high |
| CWE-89 | SQL injection | `webserver` | keywords: `UNION+SELECT`, `' OR '1'='1`, `SLEEP(`, `BENCHMARK(`, `information_schema`, `xp_cmdshell` | high |
| CWE-79 | XSS | `webserver` | keywords: `<script`, `onerror=`, `javascript:`, `%3Cscript` | low (noisy; FP note mandatory) |
| CWE-502 | Unsafe deserialization | `webserver` | keywords: `rO0AB` (Java b64), `aced0005` (Java hex), `TypeObject`, `ysoserial`, `__VIEWSTATE` anomalies | high |
| CWE-94, -95 | Code injection / SSTI | `webserver` | keywords: `eval(`, `assert(`, `base64_decode(`, `{{`+`}}`, `${`+`}` template probes | medium |
| CWE-434 | Unrestricted upload | `webserver` | keywords: upload paths with `.jsp`, `.jspx`, `.php`, `.aspx`, `.ashx`, `.war` extensions | high |
| CWE-918 | SSRF | `webserver` | keywords: `169.254.169.254`, `metadata.google`, `localhost:`, `127.0.0.1`, `file://`, `gopher://` in params | high |
| CWE-611 | XXE | `webserver` | keywords: `<!ENTITY`, `SYSTEM+"file://`, `SYSTEM+"http://` | high |
| CWE-287, -288, -306 | Auth bypass | `webserver` | admin/API paths returning 200 from external sources; keywords per-product — mark `briefr_note`: requires environment tuning | medium |
| CWE-416, -787, -119, -122 | Memory corruption | `application` (crash logs) | Windows EventID 1000/1001 for the affected product image; segfault patterns on Linux | low (crash ≠ exploit; FP note mandatory) |
| CWE-798 | Default/hardcoded credentials | `authentication` | successful logon by vendor-default account names for the affected product; new-source logins for service accounts — mark `briefr_note`: fill product default usernames | medium |

3. Every CWE-derived rule keeps `status: experimental`,
   `briefr_confidence: MEDIUM` (LOW for the "low" rows), and the existing
   validate-before-deploy note. Tests: technique-mapped CVE still uses the
   technique template; technique-less CVE with CWE-22 gets the traversal
   template with `briefr_basis: cwe`; CVE with neither gets the generic
   template with `briefr_basis: generic`.

## Spec B3 — Phase 3 self-review checklist (apply before opening the PR)

After splitting `database.py` per `docs/REFACTOR_PLAN.md`, verify:

1. `pytest tests/ -q` collects the **same test count** as before the split
   and passes; `python -c "import database"` works.
2. No function is defined twice (moved AND left behind):
   `grep -c "^async def\|^def " backend/database.py` must match the shim's
   import surface — the shim contains only imports, no logic.
3. Module-level state moved intact and exactly once: locks
   (e.g. `_pulse_ioc_lock`), constants (`NVD_SYNC_WATERMARK_KEY`,
   `EPSS_BACKFILL_DONE_KEY`, `ATLAS_UPSTREAM_VERSION_KEY`), and any
   caches — one owning module, others import from it.
4. No import cycles: every new `db/*.py` module imports cleanly on its
   own (`python -c "import db.cve, db.enrichment, db.cache, ..."`).
5. Zero SQL or logic edits mixed into the move — the diff is
   cut/paste + imports only. Any bug found during the move is a separate
   follow-up PR, never fixed silently inside the refactor.
6. `git diff --stat` shows `database.py` shrinking to a thin shim and no
   unrelated files touched.

## Spec H — reusable UI component layer (design decided 2026-07-06)

Consolidates the good half of the codebase (`pages/admin/shared/*`,
`PdfExportModal`'s a11y, `Toast`'s API) into one primitives layer instead
of introducing a component library. Non-goals: no CSS/component
framework, no TypeScript, no data-fetching library, no visual redesign —
primitives adopt the existing dark-terminal look via `App.css` tokens.

0. **Re-verify before executing (first step of every H item).** The
   file/component references below were verified against code on
   2026-07-06 and may drift. Confirm the named files still exist in the
   described shape — `pages/admin/shared/` (AsyncSection, ConfirmModal,
   HelpTip, Toast, ToggleSwitch, StatCard), `PdfExportModal.jsx`,
   `hooks/useModalLayer.js`, the CVEFeed `feed-state` blocks, `api.js`
   `request()` — before changing anything. If code has drifted (files
   moved, B4 landed, behavior already added), adapt the item to reality
   and record the deviation in Track H; if the drift invalidates a
   decision below, stop and flag instead of improvising.

1. **Layout:** primitives live in `frontend/src/components/ui/` — one
   component per file, default export named like the file, one shared
   `ui.css` with `ui-*`-prefixed classes (no collisions with legacy
   per-feature classes during migration), `index.js` barrel. `useAsync`
   goes in `src/hooks/`. Import rule: `ui/` never imports from
   `components/`, `pages/`, `context/`, or `api.js` — imports point
   downward only.

2. **Token additions to `App.css`** (primitives never hardcode these):
   `--focus-ring`, `--radius-sm/-md`, `--motion-fast` (inside the
   120–180 ms band), `--z-drawer/-modal/-toast` (today's magic
   z-indexes, named). The existing global `prefers-reduced-motion` rule
   stays the single motion kill-switch; primitives animate
   opacity/transform only.

3. **API conventions (every primitive):** controlled-by-default
   (`open` + `onClose`, no internal open state); `variant`/`size` as
   strings, not booleans; spread `...rest` onto the root element and
   merge `className`; JSDoc `@param` per prop on every export (the
   plain-JSX substitute for prop types).

4. **Key APIs (settled):**
   - `useAsync(fn, deps)` → `{ data, error, loading, refreshing,
     retry }`. Owns the AbortController and stale-while-revalidate
     (`refreshing` = loading with previous data still rendered — the
     CVEFeed `.feed-refreshing` behavior, formalized). Explicitly not a
     cache/query layer; `error` is the enriched `Error` from `api.js`
     (`.status`, `.requestId` after A2), passed through unswallowed.
   - `<AsyncState loading refreshing error onRetry empty emptyTitle
     emptyAction skeleton>` — explicit flags (no `data == null`
     inference like admin `AsyncSection`), `skeleton` as a prop,
     `refreshing` dims children under `aria-busy` instead of
     unmounting (no skeleton flash). Children: plain JSX for the
     common case, or function-as-children `children(data)` which
     AsyncState invokes only in the data state. Use the function form
     whenever inline expressions dereference `data` (e.g.
     `data.items.map(...)`) — plain JSX children are evaluated by the
     parent even while loading and would crash on `null` data.
     Component children (`<MyList data={data} />`) are safe either
     way, since creating the element does not render it.
   - `<ErrorState error onRetry compact>` — renders the safe `detail`
     message, `ref: <requestId>` as copyable text + link to the admin
     log viewer with `?request_id=` prefilled, and a retry button;
     `role="alert"`; never raw exception objects. This component is the
     A5 acceptance criterion made structural.
   - `<Modal open onClose title busy size footer>` — wraps
     `useModalLayer(open, ref, { trackDepth: true })` (focus trap,
     focus restore, overlay-depth so one Escape closes one layer);
     `busy` blocks Escape and overlay-click dismissal (the
     PdfExportModal guard, made universal); auto `role="dialog"
     aria-modal aria-labelledby`; body scroll lock. `<ConfirmModal>`
     composes it and keeps the `actionId` →
     `/api/admin/destructive-actions` lookup in an admin-side wrapper
     so `ui/` stays API-agnostic.
   - `<Badge variant explain>` — `explain` is required and renders a
     `<Tooltip>` (hover + keyboard focus + touch, `aria-describedby` +
     `role="tooltip"`, viewport-edge flipping — absorbs admin
     `HelpTip`). Raw `title=` is deprecated on status elements
     (PRODUCT.md principle 1 becomes a props contract, not a review
     catch).
   - `<Button variant="primary|ghost|danger" size busy busyLabel>` —
     `busy` ⇒ disabled + `aria-busy` + label swap; always a real
     `<button>`.

5. **A11y acceptance per component** (browser-verified, keyboard-only,
   before merge): Modal — Tab cycles inside, focus returns to trigger,
   two stacked layers close one per Escape; Toast — `aria-live` region,
   errors persist until dismissed; Tooltip — opens on focus, Escape
   hides, works on touch; AsyncState — loading `role="status"`, error
   `role="alert"`; Button — visible `--focus-ring`; contrast ≥ 4.5:1
   against both theme token sets.

6. **Migration rules:** strangler pattern — legacy classes keep working
   until each feature migrates; pixel parity while migrating (a11y
   fixes are the only allowed visual change); delete a feature's local
   CSS block in the same PR that migrates it (no orphans); DetailDrawer
   adopts primitives during its B4 split, never before.

7. **Flagged, not settled — decide at H1/H2 review:**
   - Toast location: Spec A2+A3 says `components/Toast.jsx`; the `ui/`
     layout implies `components/ui/Toast.jsx`. Either execute A3
     verbatim then relocate with a one-line re-export shim in H2, or
     amend A3 first — maintainer's call, do not improvise a third
     option.
   - Toast ↔ router coupling: keep `ui/` router-free by passing the
     ref link as an href/prop instead of importing `react-router-dom`
     inside `ui/`.
   - Icons: analyst surfaces use text glyphs, admin uses lucide-react;
     primitives take an optional `icon` node and default to the
     text-glyph style so promotions don't smuggle a second visual
     language into the feed.

---

# Zero-budget operating model

No paid API access is assumed. The loop:

1. **Specs over supervision:** judgment-heavy decisions get encoded in
   this appendix (or an ADR) once; Composer executes against them. If an
   item has no spec and needs a decision, write the decision down first.
2. **CI is the enforcer:** backend tests, Postgres job, audits, and the
   Playwright smoke run on every PR — an agent cannot merge red.
3. **Free reviewers on every PR:** the repo already uses Gemini review;
   keep it, and request a second free bot review where available. Bots
   catch mechanical issues; the specs catch design drift.
4. **Batch the strong-model sessions:** when a subscription session (like
   the one that produced this plan) is available, spend it on review of
   accumulated merged PRs + updating `docs/HANDOVER.md` + writing the next
   specs — never on typing code a spec could have delegated.
5. **Open-source flip unlocks free tooling** (Track F): most code-review
   and CI services are free for public repos.
