# BRIEFR product status

**Last updated:** 2026-08-20 (INVESTIGATE Obsidian+ local-first canvas)
**Purpose:** Single page for “what’s true in production today.” When README or beta docs disagree, this wins.

---

## Release snapshot

| Area | Status |
|------|--------|
| **Release** | **v1.5.0** — V1.5 product phases 1–3 + 5 shipped (#373–#376); Phase 4 STIX excluded |
| **License** | **Apache License 2.0** (`LICENSE`, `NOTICE`, `CONTRIBUTING.md`) — OSI-approved open source. Free for personal, commercial, and self-hosted use with attribution. Active source trees carry `SPDX-License-Identifier: Apache-2.0` headers. Repository is public on GitHub. |
| **Performance (Track I)** | **Phase 1–2 complete** (#378–#382): feed scroll isolation, parallel enrichments, bulk upsert, `/api/cves` KEV JOIN + count cache + `pg_trgm`. **Phase 3a–b shipped** (#436–#437): Pydantic JSON responses (replaced deprecated `ORJSONResponse`), keyset feed pagination (`pagination=keyset`), drawer bundle (`GET /api/cves/{id}/drawer`; each sub-fetch uses its own DB connection so parallel assembly is safe on asyncpg), shared rate-limit store (`BRIEFR_RATE_LIMIT_STORE=db`). **Phase 3 tail shipped** (#443–#444): I15 feed windowing (`content-visibility: auto`), I16 server-side stack relevance sort, `BRIEFR_SCHEDULER_ENABLED` for API-only workers. |
| **Security tail** | CGNAT SSRF block (`100.64.0.0/10`) + refresh rejects past `sessions.expires_at` (#381). **JWT role revalidation** shipped (#392). **LLM summary auth** shipped — `GET/POST /api/ai/summary` and `POST /api/investigation/summary` require a logged-in session (`require_user` + session middleware); no per-PDF password prompt. **Production posture by default (flip):** `BRIEFR_ENV=production` is now the default — `/api/docs`, `/api/redoc`, `/api/openapi.json` are disabled outside explicit `BRIEFR_ENV=development`, and production startup **requires `JWT_SECRET`** (fails closed rather than auto-generating). |
| **Database** | **PostgreSQL-first by default** — `settings.briefr_require_postgres=True` (product default), so startup refuses a non-Postgres `DATABASE_URL` unless `BRIEFR_REQUIRE_POSTGRES=0` opts out explicitly. SQLite survives only as the zero-config dev/test escape hatch (`DATABASE_URL="" BRIEFR_REQUIRE_POSTGRES=0`); the production path is Postgres-only. **Intel/app schema split (036):** `intel` + `app` schemas; one-time migration on first upgrade — `docs/INTEL_PUBLISH.md`. **IOC digest indexes (043):** `ti_mirror_iocs` / `otx_pulse_iocs` use fixed-width `ioc_value_digest` + `lower(ioc_type)` btree indexes so oversized PhishTank/URLhaus URLs do not hit PostgreSQL’s ~2704 B index row limit; Alembic `043_ioc_value_digest` downgrade is intentionally unsupported when oversized rows exist — re-sync catalog mirrors after upgrade instead of rolling back. **Intel snapshot v2:** `pg_dump --schema=intel`, import `--mode merge` (upsert intel, preserve `app.*`), `publish_intel_snapshot.py`, admin Database → Intel snapshot. Pool `search_path = app, intel, public`. **`DATABASE_POOL_COMMAND_TIMEOUT_SECONDS` is SQL-only** (default 60s). **Intel snapshot:** export/import/verify scripts + `schema_row_counts.py` / `verify_schema_split.py`. Postgres CI: `test_intel_snapshot_export.py`, `test_intel_snapshot_merge.py`. **Backup round-trip:** `test_backup_roundtrip_postgres.py`. |
| **Auth** | Built-in app login + sessions (first-run `/api/auth/setup`); **analyst `/api/*` routes require a valid session** (`briefr_at` cookie; #441 — matches React `RequireAuth`) and **live is_active check** (#449). Admin/refresh routes require the **admin role** (Sprint A0). Password changes immediately revoke all active sessions (#449). Legacy `BRIEFR_ADMIN_API_KEY` removed. Wallboard token is **header-only** (`X-BRIEFR-Wallboard-Token`; `?token=` removed, Sprint A7). Optional Cloudflare Zero Trust at **edge** (operator policy, not in app code). **FE refresh dedupe (#731):** `api.js` exports shared in-flight `refreshAccessToken()` so concurrent 401 retries share one `POST /api/auth/refresh` (reuse detection otherwise revokes all sessions). `AuthContext` bootstrap uses `fetchMe()` only — never a bare `/auth/refresh` fetch. **Cross-tab lock:** `navigator.locks` serializes rotation and probes `GET /api/auth/me` first so a second tab does not replay a rotated refresh cookie (Remember me / 30-day `briefr_rt` was getting wiped by two open tabs). |
| **Rate limits** | Token buckets on IOC, refresh, admin, auth; set `RATE_LIMIT_ENABLED=1` in production. **Multi-worker:** optional shared store via `BRIEFR_RATE_LIMIT_STORE=db` persists buckets in `sync_state` (#437); set `BRIEFR_SCHEDULER_ENABLED=0` on API-only workers (#444). Scheduler runs on one owner only — see `OPERATIONS.md`. |
| **API queue** | Outbound API serialization (#221) for NVD/OTX/etc.; `/api/health` exposes per-source task-level queue status (#341). Feed sync paths pass `operation` + safe `context_id` so rows show analyst copy (not generic “Outbound API request”). Header and **Admin status bar + Overview** show the same **Background sync** indicator; rate-limit **Retry in** countdowns update every second between polls. Indicator groups by provider, caps panel height with scroll, summary counts distinguish **waiting** vs **queued**, and opens in a **portaled Radix dropdown** (collision-aware; no clipped absolute panel). |
| **FEED refresh honesty (Program A)** | FEED footer “next refresh” reflects **NVD incremental sync** only (`nvd_incremental_sync` interval). Orphaned `CACHE_REFRESH_HOUR` / `CACHE_REFRESH_MINUTE` env vars are **not scheduled** — no APScheduler job reads them; `/api/health` `refresh_schedule` is `null`; admin config help and `.env.example` state unused/reserved. Removed misleading “auto daily …” footer copy. |
| **DB integrity** | SQLite uses `PRAGMA integrity_check` / `foreign_key_check`. PostgreSQL uses real `pg_catalog` probes (invalid indexes, unvalidated constraints, FK orphan scan) — no silent always-ok stub. Admin Overview “Check DB integrity” reports `method` + `backend`. |
| **UI (Track E)** | Track E complete (E-PR1–10): Intel/GreyNoise drawer; BRIEF states audit; header/timezone; tooltips; stat deltas; IOC auto-detect; **⌘K command palette**. |
| **UI design system (UI-M)** | **Phase 0 E0-1/E0-2/E0-3 shipped:** `tokens.css` wired; semantic tokens + legacy aliases; design-token lint in `verify-local.sh`; tool-wide motion toggle (`data-motion`). **Showcase UI is the default** (`ui_variant: pitch`); Admin → Display **Newspaper Style** restores the classic dense terminal layout (`default`). Per-user via `GET/PATCH /api/me/preferences`; admins can set an instance default (`GET/PUT /api/admin/display/ui-variant-default`). Showcase CSS (`pitch-variant.css`) covers feed filters, common vendors, sidebar techniques, IOC quota panel, and brief chart chips. **Radix primitives (E3 complete):** `Checkbox`, `Switch`, `Dialog`/`AlertDialog`, `Select`, `Tabs`, `DropdownMenu`, `Slider`, plus composites **`Badge`**, **`EmptyState`**, **`StatCard`**, **`Card`**, **`Pill`**, **`Toast`** in `components/ui/`. Zero native `<select>` / `<input type="range">` (grep gates). **E8 (IA) complete:** `--accent-selected` active states (#598), admin breadcrumbs (#599), overview needs-attention landing (#600). **Brand accent:** `--c-accent` / `--accent-selected` = `#e85533` (BRIEFR orange; unified with admin, 2026-07-15). **Reliability (E1-1/E1-2):** correlation snapshot precompute behind `CORRELATION_PRECOMPUTE_ENABLED` (default off); `POST /api/cves/{id}/risk` no longer blocks on correlation — OP hero renders from cheap signals immediately; campaign escalation merges when correlation loads. **E7-5 shipped:** All Chart.js charts migrated to **Recharts** behind `ChartShell` (shared lazy chunk ~98 kB gzip). EPSS sparklines remain custom SVG. Chart.js removed from `package.json`. |
| **FEED query language** | Deterministic search-box parser (no LLM): `+`/space = AND, comma = OR (vendors/severities), `-` excludes vendors, prefixes (`vendor:`, `is:kev`, `sev:`, `t:`, `epss:`, `date:`, `stack:`), phrases (`kevs from apache`, `apache kevs`), CVE IDs, quoted phrases. Parsed chips under search; quick filters sync tokens. Backend: `exclude_vendors`, `severity_list` (IN), `affected_products` in keyword search. Hybrid search defers to `/api/cves` when structured filters are active. |
| **CVE Overview (analyst workflow)** | Overview headline is **Operational Priority (P1–P4)** + **Threat Score (0–100)** + **Environment Relevance tier** (ADR-002). Backend `scoring/` is the sole engine; DetailDrawer displays `POST /api/cves/{id}/risk` fields only (no live FE Threat recompute — W2). Threat is asset-independent; **CISA KEV floor 80 only** (`is_kev`); VulnCheck-only has no floor. UNKNOWN environment is provisional (no phantom 17.5 pts). **W3:** OP may escalate one band for HIGH/MED + EPSS ≥ 0.5 + env ≥ POSSIBLE, or P3→P2 on rising EPSS — Threat formula unchanged; low EPSS never de-escalates KEV/CRIT P1. **W4:** parallel **SSVC annotation** on `/risk` (`ssvc.outcome` Act|Attend|Track*|Track); chip beside OP; does not change Threat or replace P-band. Doc crosswalk P1↔Act, P2↔Attend, P3↔Track*, P4↔Track (OP remains primary). **W5:** optional profile `internet_facing` / `criticality` (+ `privileged_service` / `ot_safety`) modify OP and/or SSVC only — never Threat; absent = prior behaviour. OP `operational-priority-1.2`: KEV/CRIT + internet-facing + env ≠ NO_MATCH prefers P1 over P2. Asset wizard Environment step has optional scoring toggles. v1.1b blend retained in API as `legacy_risk_v11b` only. Correlation OP escalation may still merge client-side after correlation loads (temporary; documented in SYSTEM_DESIGN). Overview order is **Description → twin Operational Priority / Environment Relevance row → Why this matters → Severity context → Key exploitation signals → Exploitation → Affected products (+ CWE) → Remediation → References → CAPEC / SSVC / OSV enrichment**. Investigation Score route removed. |
| **Data utilization (C2)** | Drawer **CAPEC** chips (CIRCL), **CISA SSVC** section (Vulnrichment), **KEV ransomware** badge on feed cards + drawer; OTX targeted countries; OSV drawer table; EPSS percentile. **OTX pulses** on CVE detail: `feed_cache` (6h) → `otx_cve_pulses` (6h) → live OTX; upstream HTTP 4xx/5xx serves any-age stale mirror rows without deleting cached pulses. **OTX/campaign titles** are humanized for display via `formatIntelLabel` (underscores → spaces; author `Part N/M` badge + tooltip; raw keys unchanged for matching). **Active Campaigns** clusters related pulses by normalized base title within each source (primary card + related count; optional “also seen from …”); matching uses stronger `normalize_pulse_name` (Part/punct/`_` identity only — not display). |
| **Forge honesty (UX RCA PR-D)** | ATT&CK navigator technique names wrap (no ellipsis identity truncation). **Coverage empty/sparse:** shared `EmptyState` when no techniques (no forced tall dark columns / “black band”); populated matrix scroll caps height only when data exists. Hunt-pack detail exposes `linked_cve_total` + truncated `description` on `linked_cves`; rail shows **CVE inventory** as primary (Generate pack demoted). Campaign clusters: no “ranked for your stack” without stack/pins; empty guidance + optional **Browse global (unpersonalized)**; member CVE inventory replaces plural OPEN CVEs→one-CVE CTA. Wallboard coverage empty copy distinguishes global vs stack-scoped. |
| **DetailDrawer chrome (UX RCA PR-A)** | Overview reading order Description → OP\|Env twin → …; panels use L-edge + accent-ghost actions (incl. GreyNoise load); non-red CVE links; Active Campaigns default closed; MITRE hint only when techniques exist; section headings without `//` (empty/loading may keep `//`); Overview/Intel/Detect/Related stay mounted via `hidden` after first visit so expand/scroll survive tab switches. Global shortcuts ignore modifiers + text selection; sticky hover cleared on shell tab change. |
| **Detection (D1)** | BRIEFR class templates use **CWE** when no ATT&CK technique is mapped (`briefr_basis`: `attack_technique` \| `cwe` \| `generic`). Generic dumps are **not emitted**. |
| **Detection (D2)** | **`DetectionContext`** scaffold: `feed_cache` keys `detection_ctx:{cve_id}` hold `{cwe_ids, product, class, artifacts, model, provider, generated_at}`; read on detection/forge paths; written by scheduler job (`DETECTION_CONTEXT_SYNC_ENABLED=0` default). Generated rules add `briefr_class` when context is present. |
| **Detection (D4)** | Deterministic **Nuclei YAML parser** enriches `detection_ctx` artifacts on `exploit_sync` (Nuclei-touched CVEs); when a BRIEFR template *is* emitted, artifact keywords/paths merge (`briefr_artifacts`, `briefr_note`). `DETECTION_CONTEXT_NUCLEI_ENABLED=1` default. LLM extract (K4) remains optional overlay. |
| **Detection (D3)** | **Unified class router** (`class_router.py`): `_resolve_detection_class(cve)` drives Sigma `briefr_class`, SIEM query selection, and `log_patterns` so all three agree on class when no ATT&CK technique is mapped. |
| **Detection (D5)** | Detect prefers **local SigmaHQ Postgres index** (CVE-exact, DRL-1.1 attribution + Show YAML) via `find_sigma_rules`; live GitHub search only when the index is empty (`match_basis` + ranking on fallback). Empty Detect copy distinguishes never-synced / empty index / no CVE-exact match. BRIEFR template YAML is **suppressed** when community hits exist or the template would be `generic`. Scheduler job `sigmahq_index_sync` (default weekly) mirrors SigmaHQ tarball with commit+sha256 watermark. Admin: Scheduler Run now, Config enable/interval, Feed Health Sync + Force re-sync (clear watermark **and** spawn), Needs Attention + analyst schedule when empty/stale (>14d). Forge attaches index YAML when present. || **LLM router (K1–K3)** | Scheduler-side multi-provider router: Groq (`openai/gpt-oss-20b` / `120b` for PDF) → Cerebras → OpenRouter `:free` (`google/gemma-4-31b-it:free` default) → Gemini (`gemini-3.1-flash-lite` default); product extraction + PDF executive summary wired through router; Anthropic removed from chain; `feed_cache` provenance `{provider, model}`. **Model defaults (2026-07):** OpenRouter no longer lists Gemini Flash-Lite `:free` (stale IDs → `model not found`); Gemini 2.0 Flash-Lite is deprecated — catalog SSOT in `ai/model_catalog.py` / `gemini_client.py`. Clear any old `OPENROUTER_MODEL_*` / `GEMINI_MODEL` env or `app_settings` overrides so defaults apply. **Outbound payload guard:** `ai/llm_payload.py` skips provider HTTP when user/assistant messages or task source text (CVE description, exploit text, investigation items) are empty — no quota burn on blank requests; enforced in router + OpenAI/Gemini clients. **Empty-response degradation:** blank provider bodies count toward circuit health (`record_source_failure`); scheduler jobs use `llm_job_session()` so a provider that returned empty is skipped for remaining CVEs in that run while failover to the next provider still happens on the first empty. **AI operations (AI-1, #416):** each `chat_completion_task` attempt records a redacted row in `ai_operations` (no prompt text); model failover chains centralized in `ai/model_catalog.py`; disable recording with `AI_OPERATIONS_RECORD=0`; admin `GET /api/admin/ai/operations/models` exposes the catalog. **Token usage (#420):** when a provider reports usage (OpenAI-compatible `usage`, Gemini `usageMetadata`), `input_tokens`/`output_tokens`/`total_tokens` are stored; Usage and Activity tabs show rollups and per-row counts; `estimated_cost_usd` stays NULL (no price SSOT). **AI-3 quota (#432):** advisory quota snapshots from provider rate-limit response headers (`ai/quota.py`); surfaced on AI Operations provider rows and `quota_warnings` in overview. **K5 pacing (#433):** shared `ai/llm_pacing.py` headroom (~85% default) below RPM/TPM ceilings for all LLM providers; PDF user-prompt trim. **AI Operations admin page (AI-2, #417; #419):** Operator → **AI operations** — read-only Overview / Providers / Models / Usage / Activity tabs backed by `/api/admin/ai/operations/*`; Activity tab filters by task class and provider. **Program E Task 2:** Admin can inspect failed prompt payloads (`GET /api/admin/ai/operations/{operation_id}/payload`) and trigger manual replays (`POST /api/admin/ai/operations/{operation_id}/retry`) with replay provenance (`context_type="replay"`, `context_id=<original operation_id>`), circuit-open guard (`409` unless `force=true`), and `ai.operations.retry` audit entries. **Program E Task 3:** Activity rows now expose `has_payload` for opt-in payload presence, show per-row **View payload** and **Retry** actions when captured payloads exist, and Feed Health degraded cards with `empty LLM response content` point operators to AI operations for factual follow-up. |
| **LLM detection context (K4)** | Scheduler job `detection_context_llm` extracts `{paths, params, keywords, method}` artifacts from CVE/exploit text into `detection_ctx:{cve_id}` via LLM router (`DETECTION_CONTEXT_LLM_ENABLED=0` default). Vision (Cerebras `gemma-4-31b`) deferred until image inputs exist. |
| **Cache retention (C3)** | Daily `cache_retention_cleanup` job sweeps stale `ioc_cache` / `feed_cache` rows and ages out `epss_history`, `cve_change_history`, and OTX mirror tables; read-path TTLs unchanged. Admin `change_history_old` purge fixed (`detected_at`). Operator append-only tables now age out in the same job (**#418**): `ai_operations` (30d) and `webhook_delivery_log` (30d) as high-frequency logs, `audit_log` (365d) as a compliance-conservative window; `api_call_events` (30d, Q2); `api_usage` is excluded (a bounded `(service, date_utc)` aggregate). The same job now also sweeps **crash-stranded webhook dedupe claims** (IDEM-D) — a dedupe row with no successful delivery-log entry, older than a 1h grace but within delivery-log retention — so a worker crash between claim-commit and delivery can't silently suppress an alert forever. |
| **Admin** | Security, backups, job status, config (V1.4 operator features largely shipped). **Operator admin bundle (#428–#429):** `AdminDataGrid` on Feed health + Storage; collapsible config sections + sticky apply bar (O-1/O-2); compact purge cards; Storage adds per-table sizes, host disk I/O, growth estimate; Download DB removed from UI (legacy `GET /storage/export` remains for SQLite dev); audit/config secret masking (`redact.py`; legacy `audit_log` targets masked on read via `mask_audit_log_target`). **Wallboard kiosk config (O-3, #514):** `WALLBOARD_TOKEN` in `config_schema` under Security / kiosk on API keys & config — save/rotate via config apply with masked GET. **API key health (#435 + Issue 21 UI):** scheduler `api_key_health_check` job + `GET /api/admin/api-keys/health`, manual `POST .../run`, and Admin → API keys provider health table (suffix, status, Run check now). Probes persist to `api_key_health:{provider}` only — they do **not** open/clear shared Feed Health circuits used by real LLM/ingest traffic (`record_circuit=False`, `ignore_circuit=True`). **Notification center (#439):** shared `NotificationBell` backed by `user_notifications` and `/api/me/notifications` — analyst header bell (watchlist/IOC alerts, and `kev_backlog` — a new detection-backlog gap on the operator stack, emitted scheduler-side from `detection/backlog.py`, deep-links to Forge `?view=backlog`) and admin StatusBar bell (job errors, unhealthy API keys, and webhook delivery failures — deep-links to Webhooks/API keys/Scheduler); open clears badge (`seen`); dismiss removes after 5s undo; chime on new unread with mute in Admin → Display (`notification_sound`). Legacy `GET /api/admin/notifications` audit feed remains for diagnostics. **Typography:** shared `--type-*` scale enforces a 12px minimum; admin base font matches analyst body; Brief/feed/admin stat surfaces migrated off sub-12px literals. **Read-only DB explorer (PR13):** Admin → **Storage** includes a table browser backed by `GET /api/admin/db-explorer/tables` and `GET /api/admin/db-explorer/tables/{table}/rows` — dropdown-driven allowlisted tables only (deny-by-default; `users`, `sessions`, `webhook_destinations`, `ioc_cache`, `sync_state`, etc. return 404); optional single-column equality filters; `cves` requires a `cve_id` filter; Tier-2 masking on `audit_log` and `webhook_delivery_log`; paginated (max 100 rows); browse audited without row bodies; 30/min rate limit. **Webhook destinations API (PR12b):** `POST/DELETE /api/admin/webhooks/destinations` for database-backed multi-destination CRUD (20/kind cap); masked `config` on GET; PATCH `config` for db sources only; event dedupe is per `(destination_id, event_type, dedupe_key)`; admin webhook test works on disabled destinations. **Webhooks admin page (PR12c):** primary destination management UI — list/create/delete db destinations, env/db source badges, event subscription editor, per-destination test; API keys page webhooks section is legacy bootstrap only. **Webhook destination health:** card grid with left-border health state, last success/failure, 24h ok/fail counts, masked `last_error`; `GET /api/admin/webhooks/health`; delivery-log `error` masked on read. **Config apply lifecycle:** `GET /api/admin/config/schema` exposes `apply_strategy` per key (`immediate`, `scheduler_reschedule`, `restart`); Admin → API keys & config shows human `display_label`, unit suffixes on intervals, and badges for reschedule vs restart; scheduler interval saves reschedule APScheduler triggers without a full restart; `ALLOWED_ORIGINS` is honestly marked restart-required (CORS middleware binds at startup). **Structured logging:** every scheduled job runs inside `job_log_context` — ring-buffer entries carry `job_id` + `run_id`; failures include `error_type` + `exc_info`; Admin → Application logs filters by job/run; Scheduler job table links “View in application log” to the last run. **Ops charts:** System health ingest durations use horizontal bars with explicit `s`/`min`/`h` units; backup sizes render as a sparkline with `fmtBytes` Y-axis; empty chart wells collapse compactly. **Analyst charts:** KEV due-date histogram replaced with `GET /api/stats/top-vendors` horizontal vendor bar chart; unified `daysUntilDue` UTC math in `kevDeadline.js`. **Admin density:** compact `admin-empty` rows; destructive `DangerZone` panels moved below operational tables with subdued styling; Security page documents optional `WALLBOARD_TOKEN` gate. **Responsive analyst surfaces:** IOC lookup uses fixed single-line input; feed filter bar and detail drawer tighten at ≤960px. **First-hour onboarding:** Admin → System health shows a live checklist (CVE ingest, stack, backups, feeds, production posture) until dismissed or complete. **Support pack:** Admin → System health “Export support pack” and `GET /api/admin/diagnostics/support-pack` export redacted health + ring-buffer logs (no secrets); `deploy/briefr-doctor.sh` runs health/import checks and optional pack download. **Corpus drift (PM-3d):** `POST /api/admin/diagnostics/corpus-drift` regenerates the security architecture generated layer to a temp dir and reports drift vs committed corpus; Admin → System health **Check corpus drift** button. CI drift tests remain in `test_security_architecture_corpus.py`. **Operator settings:** writable admin config keys persist to `app_settings` in DB (survives `.env` refresh on deploy); process env wins, then DB, then `.env`. Admin Save still mirrors to `.env` for compatibility. **Secret-typed** keys are encrypted at rest in `app_settings` when `BRIEFR_SETTINGS_KEY` is set (ADR-006); without it, secrets stay `.env`/process-env only and are not written to the DB. **Config editor:** per-field Save on Admin → API keys & config — immediate save for API keys/toggles; rows tagged `restart` use Save & restart (Wave 1). **Notifications:** shared toast tray pauses auto-dismiss on hover/focus; errors/warnings persist until dismissed; success/info auto-dismiss in 8s; max 4 visible; copy-ref shows “Copied” feedback; backend restart shows a top banner (health poll) instead of a transient restart toast (Wave 1 PR 2 / H1a). **User stack:** Feed stack input persists via `GET/PUT /api/me/stack` (Wave 2 PR 3–4); legacy `briefr_stack` localStorage migrates on login. KEV-on-stack webhooks and wallboard use `BRIEFR_STACK_TERMS` when set, otherwise the newest saved user stack. **Display prefs:** Admin → Display settings and header timezone persist via `GET/PATCH /api/me/preferences` (Wave 2 PR 5); legacy `briefr_*` display/timezone localStorage migrates on login. **Typography px:** per-role 9–20px dropdowns with Apply (session preview), Save as my default, and admin Save as instance default (`GET/PUT /api/admin/display/typography-default`); effective sizes applied via `--type-*` CSS variables. **My Stack inventory:** session-only by default; optional “Remember on server” toggle (Wave 2 PR 6) stores asset profile JSON on `user_preferences` and restores on sign-in. Production posture self-check (Sprint A6): startup logs one warning per unsafe flag (`RATE_LIMIT_ENABLED=0`, `AUTH_COOKIE_SECURE=0`, tokenless wallboard) when `BRIEFR_ENV=production`; the Security panel shows the same warnings. |
| **Admin operator hardening (A–H)** | **Complete on `main`** (#776–#781, #783, #784): ops chart axes, admin scroll/metering/tokens, host capacity + efficiency audit, API call audit trail + CSV, outbound pacing tiers + LLM failover + custom AI providers, IOC/responsive polish, backend/frontend perf batching, live host CPU, PDF footer fix. Phase H gates: full pytest + frontend unit + build + corpus drift. **Deps:** fastapi **0.140.13** (#784; security corpus `self_stack.yaml` synced). |
| **Operator config flags** | Admin config schema and `GET /api/admin/config` now expose env-backed booleans for `CORRELATION_PRECOMPUTE_ENABLED`, `DETECTION_CONTEXT_SYNC_ENABLED`, `DETECTION_CONTEXT_LLM_ENABLED`, `DETECTION_CONTEXT_NUCLEI_ENABLED`, and `SIGMAHQ_INDEX_SYNC_ENABLED` (plus `SIGMAHQ_INDEX_SYNC_INTERVAL_HOURS`). `CORRELATION_PRECOMPUTE_ENABLED`, `DETECTION_CONTEXT_SYNC_ENABLED`, `DETECTION_CONTEXT_NUCLEI_ENABLED`, and `SIGMAHQ_INDEX_SYNC_ENABLED` are immediate-apply (scheduler/request paths read them on each tick). Interval keys use `scheduler_reschedule`. `DETECTION_CONTEXT_LLM_ENABLED` is `restart` apply strategy because the `detection_context_llm` job is registered only at scheduler startup. |
| **Scheduler Catch-up** | **Catch-up mode v1:** Admin → Scheduler exposes an admin-only, time-boxed backlog drain. While active it raises embeddings/correlation per-run caps and LLM pacing headroom, and a `catchup_tick` job nudges eligible backlog work every 5 minutes. The tick now kicks embeddings, correlation precompute, LLM product extraction enqueue/inline path, and CPE catalog sync through their existing enabled gates and locks. `api_queue`, provider Retry-After, and rate-limit floors still apply; GPU acceleration is not included. |
| **Embeddings** | Nightly `embeddings_backfill` when `EMBEDDINGS_ENABLED=1`. **E1–E8 shipped** (#671–#675 + techniques + campaigns). **E7:** hybrid stays on with My Stack (`stack`/`severity`/`kev_only` on `/api/search/semantic`); scheduled CVE backfill re-embeds `content_hash` drift; golden query contracts. **E8:** `entity_type=campaign` for non-retracted `correlation_campaigns` (label/adversary/malware/tags); hybrid typed hits (`meta.includes_campaigns`). **FEED hybrid search (Wave 4):** `/api/search/semantic` technique/campaign hits render in sectioned FEED results (Techniques / Campaigns / CVEs) instead of being dropped client-side. **Retrieval ops:** `GET /api/admin/retrieval/health` + AI ops panel (live `embeddings` counts by entity, pending, extension, last backfill). `EMBEDDINGS_AUTO_ON_INGEST` **defaults on** (still requires `EMBEDDINGS_ENABLED`); Admin enable of embeddings couples auto-on-ingest=1; operator can set auto off alone. Caps: ingest max 25, backfill max 2000. Retrieval only — no RAG. |
| **Investigation graph** | **P0 APIs shipped:** session-gated `GET /api/investigations/resolve`, `GET /api/investigations/entities/{type}/{id}`, and `GET /api/investigations/entities/{type}/{id}/relationships` return frozen `GraphPage` JSON (nodes, edges, `edge_class`, `source_key`, caps, `truncated`, keyset `next_cursor`) over existing CVE/IOC/campaign/technique/Sigma/TI-mirror/**publication** tables — no graph DB, **no outbound enrichment on expand**. **`GraphNode` frozen** (`extra=forbid`): no KEV/EPSS/CVSS on nodes — **OPEN CVE** for scoring. **P1 canvas shipped:** header tab **INVESTIGATE** (`?tab=investigate&q=` deep-link resolves on load) — search, force-layout graph, **architecture camera** (non-passive wheel zoom, drag pan, `+`/`−`, **FIT GRAPH** / **RESET VIEW**), **click inspect** / **double-click expand** (`sigma` has no EXPAND). Evidence inspector lists incident edges with `edge_class` · `source_key`; pivots include **LOOKUP LIVE** (IOC with parsed `IocKind`), OPEN CVE, Forge/Advisories, PIN THREAD. Chrome: **Related CVEs** opt-in (local-first core neighborhood + counted banner), entity-type chips, edge-class chips, **Isolate**, **Find** (fly-to), optional **Semantic** edges + **LOAD MORE** on truncated neighborhoods. Camera: smoothed zoom, pan inertia, node drag with physics. Truncation/degraded copy; optional pin to session thread. Client accumulates expansions with hard caps (**200** nodes, **300** edges; root preserved); `truncated`/`capped` sticky when UI trims. Entity paths percent-encoded. No saved cases. |
| **Incidents & News** | Five RSS sources (The Hacker News, Krebs on Security, Dark Reading, Schneier, CISA Advisories) + MITRE ATLAS via `GET /api/case-studies/feed`. Bleeping Computer removed from ingest. **RSS↔CVE:** news cards include `cve_ids` parsed from title/body; Incidents UI chips open the CVE drawer; drawer RELATED tab shows matching articles (`related_news` on drawer bundle). |
| **Advisories & Intel (publications)** | Header tab **ADVISORIES & INTEL** (`tab=atlas`) with sub-nav **Headlines | Advisories | ATLAS** (`view=headlines|advisories|atlas`). Durable structured publications via `GET /api/publications` + `GET /api/cves/{id}/publications`; ingest job `publication_source_sync` (`PUBLICATION_SYNC_ENABLED=0` default). Pilot source: CISA RSS (`cisa-news`). Drawer Intel tab lists linked publications. Does not increment `corroboration_k`. |
| **Wallboard** | **v2 shipped (#430):** session-cookie token storage, responsive tile grid, stack-aware KEV tile, mono terminal styling. Token via `X-BRIEFR-Wallboard-Token` header or admin config `WALLBOARD_TOKEN` (#514). Optional `?density=compact` URL mode or header **Compact** toggle for denser 4K kiosk layouts. **Auto-token (#843):** when `WALLBOARD_AUTO_TOKEN=1`, logged-in analysts bootstrap kiosk via `POST /api/wallboard/token` (short-lived issuance JWT + session cookie); scheduler rotates the active kiosk token on `WALLBOARD_TOKEN_ROTATION_HOURS`; admins can `POST /api/wallboard/revoke` or `/rotate`. Status strip shows DB sync age, poll interval, circuit count, and feed errors. **Top risk (W2):** ranked by Operational Priority then Threat; items expose `threat_score`, `op_band`, and `risk_score` (Threat alias). |
| **Snooze** | Removed from UI (#137). **Watchlist monitor alerts** ship via `watchlist_alert` webhooks when pinned CVEs enter KEV or show EPSS/PoC changes (scheduler job `watchlist_monitor_alerts`). |
| **Theme** | Dark only. |
| **Shell URL nav** | Analyst tabs sync `?tab=brief\|feed\|ioc\|investigate\|atlas\|forge` (Forge still uses `view`/`technique`/`pack`). Admin sidebar syncs `?p=`. Intentional context changes (**tab**, Forge view/technique, Admin page, Forge→CVE, FEED→Forge) **push** history so Browser Back restores Forge/FEED; cosmetic hygiene (first-paint `tab=`/`p=`, stale Forge params, one-shot `?ioc=` strip) **replaces**. Open drawer syncs `?cve=`; close/Back clears it (Back closes drawer first). Notification/Intel pivots use router `navigate`, not `location.assign`. |
| **Durable jobs (Q1)** | Procrastinate foundation behind `PROCRASTINATE_ENABLED` (default off). Alembic `028` applies schema; in-process worker on lifespan when enabled; `GET /api/admin/jobs/outbound` lists allowlisted job fields and `POST /api/admin/jobs/outbound/ping` defers a singleton `jobs:health_ping` canary from Admin. Admin → Data refresh schedule shows **Durable outbound jobs** (`OutboundJobsPanel`) with loading/empty/error/data states, Refresh, and Ping queue controls; distinct from header `ApiQueueIndicator` (in-memory HTTP pacing). `jobs:llm_product_extraction` now runs through Procrastinate when enabled, with singleton `queueing_lock` and bounded retry for retryable timeouts; Admin Run/Retry also defers the durable job with `trigger="manual"` and elevated priority. Disabled/unavailable durable queue keeps the prior inline scheduler path. In-memory `api_queue` pacing unchanged. |
| **API metering (Q2)** | Every `resilient_request` attempt writes `api_call_events` (source, host/path template, status, latency, actor/job/run/request ids). Rollups + `api_usage.last_called_at` update on the same path. Admin → API keys shows 24h by-source / by-actor breakdown (`GET /api/admin/api-usage/metering`). Retention 30d. Flag: `API_CALL_EVENTS_ENABLED` (default on). |
| **CPE catalog (Q3)** | `software_catalog` from NVD CPE 2.3 API (scheduler `cpe_catalog_sync`, `CPE_CATALOG_SYNC_ENABLED=0` default). Checkpointed full then incremental sync; respects NVD pacing + metering. `GET /api/stack/catalog/suggest` (≥3 chars) for stack typeahead; Asset wizard merges server hints with curated lists. |
| **Stack Tier A (Q4)** | Opt-in historical NVD+KEV+EPSS backfill for shallow stack coverage (`STACK_BACKFILL_ENABLED=0` default). `GET /api/stack/coverage`, `POST /api/stack/backfill/agree`, run status/resume. Checkpointed per product; FEED gap banner + Agree. Deep intel stays on background jobs. **Idempotent (IDEM-A/B):** a duplicate resume/retry can't double-run a run — an atomic `claim_run_running` gate admits one worker (a crash-stalled `running` run is reclaimable after `STACK_BACKFILL_STALE_SECONDS`, default 900s); the Procrastinate defer carries a per-run `queueing_lock`. NVD rate-limit deferrals now schedule a durable resume with the same per-run lock after 180s when Procrastinate is available; otherwise the run stays deferred with manual-resume copy. |
| **Security architecture live risks** | Risk Register live self-stack rows use structured CPE / `affected_products` scoring against the generated self-stack, including version pins when available. Product+version matches score 100; product-only fallback rows score 55 and are shown as version unverified. Description text `LIKE` is not used for self-stack admission. `/api/security-architecture/section/risks` now returns additive `live_self_stack` cap-honesty stats (`candidate_rows`, `scored_matches`, `admitted`, `cap=50`), and the UI shows "live self-stack showing X of Y (cap 50)" when scored matches exceed admitted rows. |
| **EPSS identity skip (Q5)** | Scheduled EPSS sync downloads FIRST CSV.GZ and compares `sha256` + `score_date` to `sync_state.epss_csv_file_identity`. Unchanged → skip gunzip/parse/upsert/snapshot. Force: `POST /api/admin/feeds/epss/force-resync`. |
| **Docker compose** | Postgres compose exists; full V2.0 platform compose not shipped. |

---

## Auth layers (two independent)

![Auth layers](assets/auth-layers.svg)

> **Asset:** [`assets/auth-layers.svg`](assets/auth-layers.svg) — see [IMAGE_BRIEFS §8](IMAGE_BRIEFS.md#8-auth-layers)

| Layer | What | Notes |
|-------|------|-------|
| **Edge (optional)** | Cloudflare Tunnel + Zero Trust email OTP | Protects public hostname; not embedded in FastAPI |
| **Application** | Username/password + server sessions; admin routes check the `admin` role | Portable self-host; CF JWT middleware **dropped** (#93); legacy admin key **removed** (Sprint A0 — it failed open when unset). Production now **fails closed** without `JWT_SECRET` (guard reordered ahead of dev auto-generation — F7.1). |

---

## Deployment reference

![Production architecture](assets/production-architecture.svg)

> **Asset:** [`assets/production-architecture.svg`](assets/production-architecture.svg) — see [IMAGE_BRIEFS §1](IMAGE_BRIEFS.md#1-production-architecture)

| Item | Value |
|------|--------|
| Code | `/opt/briefr` |
| DB | PostgreSQL 16 (often Docker at `/opt/infra/postgres`) |
| Backups | `/var/lib/briefr/backups` (age-encrypted) |
| Backend | `briefr-backend.service` → uvicorn :8000 |
| Frontend | `frontend/dist` via nginx |

---

## Shipped vs planned (high level)

| Shipped | Planned / open |
|---------|----------------|
| Postgres, auth, rate limits, API queue, shared rate-limit store (#437) | Full `docker-compose.yml` (V2.0) |
| Correlation v3 complete (#473…#513) | STIX export (excluded from current loop) |
| Forge redesign FR-1…FR-3 (#490, #492, #495) | ONBOARDING refresh (ongoing) |
| Threat modeling TM-0…TM-5 (#491, #493–#497); **TM-6 CWE/OWASP/CAPEC/STRIDE framework workspaces** | TM-6 NIST CSF / ASVS (operator control-backed, follow-up); self-monitoring/remediation job |
| Admin ops, webhooks (#413–#415), wallboard v2 (#430), O-3 kiosk config (#514), AI ops (#416–#420), 12-PR operator bundle (#428–#439), 4-PR tail (#441–#444), LLM summary auth; **admin operator hardening A–H** (#776–#781, #783, #784) | |
| **Threat-intel blocklist export** (#836, #817) — admin-only TXT/CSV/JSON download with format + content selectors (`domains` subtracts Tranco/genuine hosts; `urls` keeps exact malicious URIs on shared infra); Feodo + PhishTank catalog mirrors; Tranco top-1M genuine-domain import (compact rows); Tranco HTTP 5xx fails the job (`had_error`); a successful non-empty snapshot expires superseded `tranco:` rows only (operator/curated hosts stay); scheduler manual-run reservation does not double-hold the job lock | |
| **PM-3 ARCH graph** (cursor zoom, fit-to-view, viewport-locked shell, strong focus dim, truncated labels, toggle-deselect, one-hop SQL edges via db helpers/`database` shim, focus-only edge draw, core/external nodes, inline node detail, admin corpus drift) | Detection composer (parked); G0–G4 end-of-lifecycle |
| **PM-4a** Admin → Security posture (`?p=securityposture`) — Overview / System Architecture / Trust Boundaries / Attack Surface / Risks; analyst read-only | |
| **TM-6** Admin → Security posture FRAMEWORKS group — CWE / OWASP Top 10 (2021) / CAPEC / STRIDE workspaces over the user's own live CVE data (Scope: All / My Stack / Watchlist / KEV + severity), projected from live `cves.cwe_ids`; every count drills through to `example_cves`, unmapped bucket keeps totals honest | |
| **PM-4b** Analyst ARCH: no Security Decisions / Reviews / Components nav; no corpus footer (API/YAML kept) | |
| **PM-4c** ARCH header tab removed; `/security-architecture` redirects to Admin Security posture | |
| **PM-4d** FORGE MITRE ATT&CK navigator — top view tabs; matrix-style tactic columns (id + name); click toggles selection; hunt pack only on coverage (clears when leaving); no GAP/COMMUNITY/YOURS chrome | |
| **PM-4e** Drawer MITRE → Forge navigator; hunt-pack CVE → drawer | |
| Detection composer **DC-1…DC-4** — evidence pack, emit Sigma/SIEM/YARA, Detect UI + Forge hunt-pack generate share engine (no LLM) | — |
| **Forge** threat scenarios, proof bench, KEV backlog, IOC watchlist (V1.5 #373–#376) | `IMAGE_BRIEFS` tail; MkDocs |
| Track I performance Phases 1–3 (#378–#382, #436–#437, #443–#444); **M-8** encrypted `app_settings` secrets (ADR-006, `BRIEFR_SETTINGS_KEY`) | |
| Track L Wave 4: monitor alerts, onboarding, doctor, operator settings (#366–#372); RSS↔CVE linking | |
| K5 LLM pacing (#433), AI-3 quota snapshots (#432), embeddings auto-on-ingest (#438) | Codebase audit tail: PR-P3, PR-O1/O2, PR-F1–F4 |
| Chart.js admin ops dashboard, logrotate deploy artifacts, F2 AGPL (#423) | QA/UX: QA-U1–U3, UX-J1, UX-L1; AKH-2 tail |
| Architecture diagrams (phase A); session auth middleware (#441); M-5 backup owner + N-4 kiosk docs (#442) | Durability: PR-R1–R4; PG-002/003 |
| UX-C1/C2 button standard (#474, #475) | Resource benchmarking: RB-1 collector ✅; RB-2 admin Resources page ✅ |

Details: https://docs.projectjupiter.in/docs/roadmap

---

## Documentation rollout

| Phase | Status |
|-------|--------|
| Doc structure + image briefs | Phase A SVGs shipped (`production-architecture`, `auth-layers`, `correlation-pipeline`, `ingest-pipeline`, `adr-001-intel-app-split`); UI screenshots in `docs/assets/screenshots/` (#755) |
| Living API / architecture docs | Refreshed 2026-07-24 — reader guides, OPERATIONS link fixes, USE.md gallery |
| Online docs | https://docs.projectjupiter.in — synced from this repo |
| Live demo | https://briefrdemo.projectjupiter.in — static 1:1 analyst UI ([`briefr-demo`](https://github.com/Soldier0x0/briefr-demo)); fixture data, no backend; Cloudflare Workers deploy |
| Self-host | [SELF_HOST.md](SELF_HOST.md) — install your own PostgreSQL-backed deployment |
| Stale README / API_REFERENCE auth claims | **Done** — session-cookie auth + refresh route errors updated |
| Security hardening (CodeQL triage) | **Done** — CI workflow least-privilege permissions; intel snapshot import path allowlist (`INTEL_SNAPSHOT_IMPORT_DIRS` / `INTEL_PUBLISH_DIR`); admin URL masking + generic 500 responses with `request_id`; log message scrubbing. **All 15 open CodeQL alerts resolved (#819):** parsed-hostname host matching (cve.py, intelligence.py, patchRemediation.js, api_queue_operations.py), urlsplit-based DB URL credential masking (database.py), no `str(exc)` in diagnostic/backup responses (diagnostics.py, storage_metrics.py), polynomial-ReDoS and incomplete-sanitization regexes replaced; **0 open code-scanning alerts on `main`** |
