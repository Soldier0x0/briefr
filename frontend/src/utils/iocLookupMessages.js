/** Analyst-facing IOC lookup messages (BRIEFR product voice). */

/** Indicator absent from all configured enrichment sources. */
export const IOC_NOT_FOUND_IN_DATABASES =
  'Not found in configured threat databases.'
