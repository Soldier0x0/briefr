# Graphify scan exclusions — global patterns (any subdirectory).
# Docs/images need GEMINI_API_KEY for semantic extraction; code is AST-only.
node_modules/
.venv/
venv/
__pycache__/
dist/
build/
graphify-out/
screenshots/
*.db
.env
.env.*

# Test fixtures and editor config — JSON/config blobs with no extractable code nodes
backend/tests/fixtures/
.claude/
